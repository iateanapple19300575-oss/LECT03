Imports Entities

Public Class MasterEditValidator
    Implements IValidator(Of MasterEditEntity)

    Public Sub Validate(target As MasterEditEntity) Implements IValidator(Of MasterEditEntity).Validate
        Throw New NotImplementedException()
    End Sub
End Class
